# 疯狂动物园 · 国内云服务器部署教程（傻瓜版）

> 目标：把网站搬到**国内**云服务器，让国内玩家稳定打开（不再依赖海外 Railway）。
> 适用：阿里云 / 腾讯云「轻量应用服务器」，系统 Ubuntu 22.04。
> 你这是**零依赖 Node 站**，迁移几乎零改造，照下面点几下就能跑。

---

## 一、买一台国内服务器（约 60–99 元/年）

任选一家，新用户都有优惠价：

| 平台 | 购买入口 | 备注 |
|------|----------|------|
| 阿里云 | https://developer.aliyun.com/act/new | 搜「轻量应用服务器」 |
| 腾讯云 | https://cloud.tencent.com/act/pro/lighthouse | 搜「轻量应用服务器」 |

**购买时按这个选：**
- **地域**：选离你近的国内城市（上海 / 广州 / 北京）
- **镜像**：选「系统镜像 → Ubuntu 22.04 LTS」（**别选应用镜像**，避免预装环境冲突）
- **规格**：2 核 2G 起步（图鉴 923 张图，2G 够用）
- **带宽/流量**：选「峰值带宽 3–5 Mbps + 月流量 200GB+」套餐
- **时长**：买 1 年最划算（新用户首单经常 60–99 元）

买完在控制台能看到一台服务器，记下它的**公网 IP**（形如 `123.45.67.89`）。

---

## 二、在控制台开放 8080 端口（关键！）

服务器默认只开 22 端口（SSH），必须手动放行 8080，否则网站打不开。

- **阿里云轻量**：控制台 → 该服务器 →「防火墙」→ 添加规则 → 端口 `8080` → 协议 TCP → 确认
- **腾讯云轻量**：控制台 → 该服务器 →「防火墙」→ 添加规则 → 应用类型选「自定义」→ 端口 `8080` → 协议 TCP → 确认

> 如果以后想用 80 端口（访问时不带 `:8080`），同样在这里放行 `80` 即可。

---

## 三、在本机把网站打包

用 **Git Bash**（装 Git 后右键「Git Bash Here」）执行，能自动排除 `.git` 等大文件夹减重：

```bash
cd /f/网站
tar -czf /f/crazyzoo.tar.gz --exclude=.git --exclude=.workbuddy .
```

打包完会在 `F:\crazyzoo.tar.gz`（约几十 MB，含全部图片）。

---

## 四、把代码传到服务器

### 方式 A：scp 命令行（推荐，最快）
Windows 10/11 自带 OpenSSH，直接打开 **PowerShell** 执行：

```powershell
scp F:\crazyzoo.tar.gz root@你的服务器公网IP:/root/
```

把 `你的服务器公网IP` 换成真实 IP，按提示输入服务器密码，等传输完成。

### 方式 B：控制台网页上传（备选）
在服务器控制台点「登录」进入 Web 终端，用里面的文件管理把 `crazyzoo.tar.gz` 拖进去（大文件可能受限，优先用方式 A）。

> 同时也把部署脚本传上去（或下一步直接从本教程复制脚本内容到服务器新建文件）。

---

## 五、登录服务器，一键部署

1. **登录服务器**：PowerShell 执行 `ssh root@你的服务器公网IP`，输密码。
2. **新建部署脚本**：在服务器上执行
   ```bash
   nano setup_server.sh
   ```
   把本教程同目录 `deploy/setup_server.sh` 的全部内容粘贴进去，`Ctrl+O` 保存、`Ctrl+X` 退出。
   （或直接在服务器跑 `scp` 把本机 `deploy/setup_server.sh` 也传上去）
3. **给脚本加执行权限并运行**：
   ```bash
   bash setup_server.sh
   ```
4. 脚本会依次：装 Node → 解压代码 → **让你输入管理员密码**（输入时不显示，输完回车）→ 用 pm2 常驻启动。
5. 看到 `✓ 部署完成！` 即成功。

---

## 六、访问测试

浏览器打开：

```
http://你的服务器公网IP:8080
```

能正常显示「疯狂动物园」首页、图鉴、到此一游，就说明成了。

> 后台管理：打开页面右上角 🛠，输入你刚设的管理员密码，即可改内容、传图。

---

## 七、绑定自己的域名（可选，需备案）

想用 `zoo.你的域名.com` 访问，需要：
1. 有一个**已备案**的国内域名（阿里云/腾讯云都可备案，免费但需几天审核）。
2. 在域名控制台做 **A 记录**指向服务器公网 IP。
3. 服务器防火墙额外放行 `80` 端口，并把 `server.js` 的端口改为 80：
   ```bash
   # 编辑启动方式：pm2 启动时设 PORT=80
   PORT=80 pm2 restart crazyzoo --update-env
   ```
   （需 root 权限，轻量服务器默认是 root）

> 没备案域名前，直接用 `IP:8080` 访问完全没问题，不影响使用。

---

## 八、以后怎么更新内容

- **在网站后台改文字/传图**：直接点 🛠 操作，数据存在服务器上，即时生效。
- **改了代码（如新增功能）想同步到服务器**：
  - 本机重新打包 `tar.gz` → `scp` 上传 → 服务器解压覆盖 → `pm2 restart crazyzoo`
  - 或把改动推到 GitHub，服务器 `cd /root/crazyzoo && git pull && pm2 restart crazyzoo`

---

## 九、常用运维命令（服务器上）

```bash
pm2 status            # 看服务是否在跑
pm2 logs crazyzoo     # 看运行日志（排错用）
pm2 restart crazyzoo  # 改完代码重启
pm2 stop crazyzoo     # 暂停
reboot                # 服务器重启后 pm2 会自动拉起服务（已设开机自启）
```

---

## 十、常见问题

**Q：打开页面是空白 / 转圈？**
- 先确认第二步防火墙 8080 已放行；
- 服务器上跑 `pm2 status` 看是不是 `online`；
- `pm2 logs crazyzoo` 看有没有报错（多半是端口被占或权限）。

**Q：scp 传不上去 / 连不上？**
- 确认服务器 IP 正确、防火墙 22 端口已放行（默认开）；
- 密码是控制台给的服务器密码，不是你平时别的密码。

**Q：想不带 :8080 直接访问？**
- 放行 80 端口，用 `PORT=80 pm2 restart crazyzoo --update-env` 即可（需备案域名才建议对外用 80）。

**Q：图片太多，流量会不会超？**
- 轻量套餐通常含 200GB+/月流量，923 张图浏览量不大时足够；若担心，可在套餐里选更大流量包。
