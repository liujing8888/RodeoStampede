apiVersion: v1
kind: Service
metadata:
  name: {{ include "rodeo-social.fullname" . }}
  labels:
    {{- include "rodeo-social.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "rodeo-social.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP